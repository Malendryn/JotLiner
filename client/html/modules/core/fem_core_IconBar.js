
`
so lets have 
<icon>, callback,

but what about dropdown for font?

we could have functions like:

id = addToolButton(.png,func)
rmToolButton(id)

or we could have 'banks'  like:

bank1 [ [icon, func]];
then addToolBank
rmvToolBank


`
